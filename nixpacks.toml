[phases.install]
cmds = ["pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = [
  "pnpm --filter @workspace/api-server run build",
  "pnpm --filter @workspace/business-jarvis run build"
]

[start]
cmd = "PORT=${PORT:-8080} pnpm --filter @workspace/api-server run start"
