Задача: собрать экран «Сегодня» для стола GR-директора (Александр).

ВАЖНО. Файл artifacts/business-jarvis/src/pages/desk/DeskToday.tsx сейчас рендерит <DeskStub/> — это заглушка. Перепиши этот файл ПОЛНОСТЬЮ: удали DeskStub, собери реальный экран. Признак успеха: на роуте "/" больше НЕТ текста «ФАЗА 1 · В РАЗРАБОТКЕ» — вместо него три блока. Проверь это сам в превью ПЕРЕД тем как написать «готово». Не отчитывайся о готовности, пока заглушка не исчезла с экрана.

Экран «Сегодня», три блока сверху вниз:

1) «Вернуться к людям» (первый, главный). Импортируй BATOV_PEOPLE из @/data/batov-people. Вычисли людей, у кого давно не было касания (по lastContactOnline / lastContactInPerson). Карточка каждого: аватар-инициалы, Имя + компания, строка «N дн без касания» (font-variant-numeric: tabular-nums; цвет: warn #f0b54a если >14 дн, alert #f0625a если >30 дн), причина = первая тема со status==='active', ряд быстрых действий высотой ≥44px (Позвонить · Написать · Отложить · 🎤). Тап по карточке → переход на /people/${id}. Сортировка: у кого дольше нет касания — выше. Показать топ-5. НЕ создавай второй источник данных — только BATOV_PEOPLE.

2) «Приоритеты». Создай файл src/data/batov-today.ts с 5–6 приоритетами, поля: title (string), due (string, срок), importance ('high'|'medium'|'low'), bucket ('day'|'week'|'month'). На экране покажи группами Сегодня / Неделя / Месяц: у каждого пункта точка-статус (high=🔴 #f0625a, medium=🟡 #f0b54a, low=🟢 #3ed9a0), заголовок, срок (tabular-nums).

3) «Неделя». Мини-календарь на 7 дней: сегодняшний день выделен цианом #00d4ff, на днях с событиями — точки. Компактный, read-only.

Плюс одно первичное действие: кнопка «🎤 Сказать» внизу-справа (учти safe-area), пока заглушка (onClick пустой).

Стиль строго из tokens.css: фон #040810, карточки #080f1e, текст rgba(228,232,255,0.92) основной / 0.60 вторичный, шрифт Hanken Grotesk. Циан #00d4ff — только акцент. Иконки — lucide-react, не эмодзи (эмодзи 🎤 в кнопке и статус-точки допустимы).

Проверка перед отчётом: pnpm run typecheck без новых ошибок; открой роут "/", убедись что заглушки «В РАЗРАБОТКЕ» нет и видны три блока; тап по напоминалке ведёт на карточку человека; проверь на ширине iPad-портрет (нет горизонтального скролла).