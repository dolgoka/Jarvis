Задача: собрать экран «Проекты-сделки» для стола GR-директора (Александр).

ВАЖНО. Файл artifacts/business-jarvis/src/pages/desk/DeskDeals.tsx сейчас рендерит <DeskStub/> — заглушка. Перепиши файл ПОЛНОСТЬЮ: удали DeskStub, собери реальный экран. Признак успеха: на роуте /deals больше НЕТ текста «ФАЗА 2 · В РАЗРАБОТКЕ» — вместо него список сделок. Проверь в превью ПЕРЕД тем как сказать «готово». После сборки сразу закоммить и запушь (git add -A && git commit && git push).

1) Создай файл src/data/batov-deals.ts с интерфейсом и 5–6 сделками. Поля каждой сделки:
   id (string), title (string), stage ('active'|'hold'|'won'|'lost'), owner (string | null — кому отдана, формат "Имя · роль"; null = «веду сам»), partnerId (string — id из batov-people.ts), amount (string | null, например "≈ $12M"), isClosed (boolean — закрытый трек), stuckDays (number | null — если застряла), lastActivity (string — например "3 дня назад").
   Возьми реалистичные фактуры и свяжи partnerId с существующими людьми: нефтетрейдинг ГПН (Мирзаев, закрытый), авиакеросин Тбилиси (Сагателян, закрытый, hold), ВЭД-аккредитив (Тураров), порт-терминал СПб (Гладышев/Протасов), UBA баскетбол-лига (Зубрилин). Одну сделку сделай «застрявшей» (stuckDays 20+) и одну won.

2) Экран /deals:
   - Заголовок «Проекты-сделки» + счётчик, справа кнопка «🎤 Добавить голосом» (высота ≥44, заглушка).
   - Чипы-фильтры со счётчиками: Все · Активные · На холде · Закрытые · Застряли.
   - Список карточек сделок, каждая:
     • title (крупно) + бейдж стадии (active=циан #00d4ff, hold=warn #f0b54a, won=ok #3ed9a0, lost=приглушённый);
     • строка «Отдана: Имя · роль» (или «Веду сам»), имя партнёра со ссылкой на /people/${partnerId};
     • сумма amount справа, tabular-nums;
     • если isClosed — маркер 🔒 «Закрытый»;
     • если stuckDays есть — заметная плашка alert-цветом «⚠ Застряла N дн»;
     • «Активность: lastActivity» приглушённо.
   - Тап по карточке пока просто ведёт на партнёра (/people/${partnerId}).

Стиль строго из tokens.css (фон #040810, карты #080f1e, текст rgba(228,232,255,0.92)/0.60, Hanken Grotesk). Циан — только акцент. Иконки lucide-react. Тап-цели ≥44. Проверка: pnpm run typecheck без новых ошибок; открой /deals — заглушки нет, фильтры работают, «застряла» видна, ссылки на людей работают; iPad-портрет без горизонтального скролла.